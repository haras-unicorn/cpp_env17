#ifndef ENV_INDEXING_HPP
#define ENV_INDEXING_HPP

// literals

WHOLE_L(index, i, FWA_STD::size_t);

let_cmp first{0_i}, second{1_i}, third{2_i};

RT_TEXT_L(name, i, const char *, string_t);

#endif // ENV_INDEXING_HPP
